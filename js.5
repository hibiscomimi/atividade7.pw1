let numeros = [4, 10, 2, 5, 7, 1];

let maior = numeros[0];

for ( let ag = 1 ; ag < numeros.length; ag++ ) {

    if (numeros[ag] > maior) {

        maior = numeros[ag];
    }
}

alert("O maior número é : " + maior);
